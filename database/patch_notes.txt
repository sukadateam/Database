1. Added a new setting. app_version_control=True
2. Created a new file called version_config.py to help handle app_version_control setting.
3. If system not macos, linux or windows, custom_database will return an error.
4. Added a new setting. set_operating_system=True
5. Added a new setting. allow_windows_version='10'
6. Fixed a problem with a function called exit(). exit() would not exit. WHY????????? I know why. That's why it's fixed.
